<div class="container">
<h1><%= t("reviews.new.add_review") %></h1>
<%= simple_form_for @review do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
    <%= f.submit t("reviews.new.add_review") %>
  <% end %>
</div>
